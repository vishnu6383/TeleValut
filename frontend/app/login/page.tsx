'use client';
import { FormEvent, useState } from 'react'; import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { api } from '../../lib/api';
export default function LoginPage(){const router=useRouter();const[identifier,setIdentifier]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setLoading(true);setError('');try{await api('/auth/login',{method:'POST',body:JSON.stringify({identifier,password})});router.push('/dashboard')}catch(err){setError((err as Error).message)}finally{setLoading(false)}};return <main className="auth-shell"><section className="auth-aside"><div className="brand"><span className="brand-mark">T</span><span>TeleVault</span></div><div style={{maxWidth:'30rem',marginTop:'7rem'}}><p style={{color:'#d7e87e',fontWeight:800}}>WELCOME BACK</p><h1 style={{fontFamily:'var(--font-space-grotesk)',fontSize:'clamp(2.5rem,5vw,4.5rem)',lineHeight:1.02,letterSpacing:'-.06em'}}>Everything<br/>in its place.</h1><p style={{fontSize:'1.1rem',lineHeight:1.7,color:'#d9e8e3'}}>Pick up exactly where you left off, with your private files close at hand.</p></div></section><section className="auth-form-wrap"><form className="auth-card" onSubmit={submit}><p className="muted">SECURE SIGN IN</p><h2 style={{fontSize:'2rem',letterSpacing:'-.04em',margin:'.4rem 0'}}>Welcome back</h2><p className="muted">Sign in to access your vault.</p><div className="field"><label htmlFor="identifier">Email or username</label><input id="identifier" required value={identifier} onChange={e=>setIdentifier(e.target.value)} /></div><div className="field"><label htmlFor="password">Password</label><input id="password" type="password" required value={password} onChange={e=>setPassword(e.target.value)} /></div>{error&&<p className="error">{error} {error.includes('verify')&&<Link className="link" href="/verify-email">Verify email</Link>}</p>}<button className="primary" style={{width:'100%',marginTop:'1.3rem'}} disabled={loading}>{loading?'Signing in...':'Sign in'}</button><p className="muted" style={{textAlign:'center',marginTop:'1.2rem'}}>New to TeleVault? <Link className="link" href="/register">Create account</Link></p></form></section></main>}
